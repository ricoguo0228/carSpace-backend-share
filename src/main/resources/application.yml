spring:
  jackson:
    date-format: yyyy-MM-dd HH:mm:ss
  mvc:
    pathmatch:
      matching-strategy: ant_path_matcher
  application:
    name: carspacesdemo
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://localhost:3306/carspaces
    username: root
    password: 20030228
  #   Redis 配置
  redis:
    database: 1
    host: localhost
    port: 6379
    timeout: 5000
    password: 123456
server:
  port: 8080
  servlet:
    context-path: /api
  address: 0.0.0.0
mybatis-plus:
  global-config:
    db-config:
      logic-delete-field: isDelete # 全局逻辑删除的实体字段名(since 3.3.0,配置后可以忽略不配置步骤2)
      logic-delete-value: 1 # 逻辑已删除值(默认为 1)
      logic-not-delete-value: 0 # 逻辑未删除值(默认为 0)

yuapi:
  client:
    access-key: knhu04kfi29402iq3dp32guj6wbvfia3
    secret-key: pxxs6hwlhgwg6v83pnq0foecialbi30t
